
  <% @lessons.each do |lesson| %>
    <li id="<%= lesson.id %>">  <%= lesson.name%>|
      <%= link_to "Добавить блоки",edit_lesson_path(lesson) %>|
      <button class="delete_lesson" id="<%= lesson.id %>"  >Удалить</button>

      <br>
  <% end %>
  </li>
