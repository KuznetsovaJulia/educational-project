<% if user_signed_in? && current_user.likes?(@course) %>
  <%=link_to "Удалить закладку", course_like_path(@course),method: :delete, remote: true%>
<% else %>
  <%=link_to "Добавить закладку", course_like_path(@course),method: :post, remote: true%>
<% end %>
<%= @course.likes.count %>